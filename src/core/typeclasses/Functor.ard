\import Function

\class Functor
  (F : \Type -> \Type (\suc \lp))
  | map {A B : \Type} (F A) (A -> B) : F B
  | covIdentity {A : \Type} (fa : F A) : map fa id = fa
  | covComposition {A B C : \Type} (fa : F A) (f : A -> B) (g : B -> C) : map fa (\lam x => g(f(x))) = map (map fa f) g